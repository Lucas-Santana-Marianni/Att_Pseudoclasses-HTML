# 🎨 Pseudo-classes em ação — Projeto de Botões Estilizados

Este projeto demonstra o uso de **pseudo-classes CSS** (`:hover`, `:active`, `:focus`) aplicadas a três botões estilizados com cores e efeitos diferentes.

---

## 🧪 O que o projeto mostra?

- Uso de **CSS puro** para criar botões interativos.
- Três pseudo-classes principais:
  - **:hover** → quando o mouse passa por cima  
  - **:active** → quando o botão é pressionado  
  - **:focus** → quando o botão recebe foco  
- Cada botão possui seu próprio esquema de cores.

---
